# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
*.egg-info/
.venv/
venv/

# 本地数据：hub 的 SQLite 库与备份不应入库（含 WAL/SHM 伴生文件）
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm

# 日志
*.log

# 系统与编辑器
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# 中枢会话凭据（含 lease_token 明文）——绝不可入库
# 2026-09-18 QA 发现：FSTDD 把 session 写在 change 目录下，而 .fstdd/ 要入库，
# 下一个闭环会把 live 的 lease_token 推上公开仓库。
.hub_session.json
**/.hub_session.json

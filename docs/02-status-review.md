# FSTDD 多机多 AI 分布式任务协作方案 — 执行情况核查报告

**日期**：2026-09-17
**场景**：分布式协作方案进度审查 / 健康检查（只读核验，未改动任何代码）
**参与成员**：排障手（investigator，实测核验）+ 产品官（product-reviewer，路径评审）

---

## 📌 TL;DR（执行摘要）

- 整体结论：🟡 **有条件通过 —— 工程缺口基本闭合，但"6 agent / 3 机"从未真正接入**
- 阻塞项数量：**2**（节点注册 0、任务分发 0）
- 一句话：数据面（裸库 + GitHub 镜像）**真跑通了**；控制面（hub）**部署完成但零业务流量**；
  节点本地层（worktree + 子 change）**完全未启用**
- 下一步：对"是否继续投入分布式接入"做一次**限时验证**（推荐路径 C），而不是继续扩建方案

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go（继续投入分布式接入） | 🟡 **条件 Go** —— 给一次 2 小时限时验证，不达标即冻结 hub |
| 严重度分布 | 🔴 2 / 🟠 0 / 🟡 2 / 🟢 6 |
| 关键行动项 | 5 条 |
| 建议负责人 | D哥（决策）+ 单一执行 agent（验证） |
| 当前瓶颈 | 不是代码缺口，是**接入执行与首个任务闭环** |

---

## 1. 各成员核心结论

### 🔧 排障手（调查员 · 实测核验）

- **核心判断**：三层架构里只有 **L1 数据面是真跑通的**——裸库 HEAD `c11789a` 与本地、GitHub
  三方 sha 收敛，mirror.log 最近 3 条全 `[OK]`、无 WARN。L2 控制面「进程活着、接口齐全，
  但业务流量为零」——`/tasks` 返回 `[]`，意味着**创建/领取/租约/心跳/消息整条链路从未被真实调用过一次**。
  L3 节点本地层零使用：无 task worktree、无 `task/*` 分支。
- **关键建议**：明确判定 **6 agent / 3 机从未接入**——`/nodes` 里唯一的条目
  `fstdd-hub-infra` 是 `deploy_server_bare_repo.sh` 自注册的运维节点，文档第 152 行自己写着"不是 agent"。
  要真跑起来缺的是：3 台开发机侧的接入动作、首个真实任务、子 change 落地，
  以及**一个承接"多机接入"的 change**（主 change 已归档，现已无人推进）。

### 🎯 产品官（产品评审）

- **核心判断**：立场鲜明地判为**过度工程**——「为 0 个真实用户设计租约回收、心跳、消息系统，
  是给假想痛点写代码」。判据是：3 台机 6 个 AI 属"未来可能有"，不是"今天已经在痛苦地绕"；
  FSTDD 真正的竞争者是 D哥 自己开三个终端手动分工，可控且零维护成本。
- **关键建议**：**不必删除**——数据面已产出真价值（唯一真值源 + 自动备份），需裁决的只有控制面 hub。
  推荐路径 **C「限时验证 + 默认冻结」**：2 小时跑一次真任务，不过就冻结。理由是 A（立刻接入）缺 kill criteria、
  会滑向平台建设，B（先修工具链）回避核心判断、只是拖延决策。

---

## 2. 综合审查发现（去重合并后按严重度排序）

| # | 严重度 | 类别 | 位置 | 问题描述 | 建议 | 来源成员 |
|---|--------|------|------|---------|------|---------|
| 1 | 🔴 | 进度 | 控制面 `/nodes` | **0 个 agent 注册**，唯一节点 `fstdd-hub-infra` 是服务器自注册的运维节点，非 agent | 停扩建；先接 1 机 1 agent 做限时验证 | 排障手 |
| 2 | 🔴 | 进度 | 控制面 `/tasks` | **任务数为 0**，创建/领取/租约/心跳/消息链路**零真实调用** | 用 1 个真实子 change 打通全链路 | 排障手 |
| 3 | 🟡 | 进度 | 本地仓库 | 无 task worktree、无 `task/*` 分支，L3 完全未启用 | 验证时一并建 worktree 走 ff-only 集成 | 排障手 |
| 4 | 🟡 | 流程 | `.fstdd/changes/` | 主 change 已归档，**「多机接入」无在跑 change 承接**；活跃 change 仅 `mirror-failure-alert`、`time-baseline` | 若决定推进，需新立 change 承接 | 排障手 |
| 5 | 🟢 | 架构 | 服务器裸库 | ✅ HEAD `c11789a`，post-receive 7478B 可执行 | 保留 | 排障手 |
| 6 | 🟢 | 架构 | GitHub 镜像 | ✅ `origin` HEAD `c11789a`，mirror.log 全 `[OK]` | 保留 | 排障手 |
| 7 | 🟢 | 服务 | `fstdd-hub.service` | ✅ active，仅听 127.0.0.1:8788，/health ok | 保留观察 | 排障手 |
| 8 | 🟢 | 代码 | `tools/fstdd_hub.py` | ✅ **非骨架**：原子领取 `UPDATE ... WHERE status='pending'`、lease_token sha256、租约过期回收、幂等键均已实现 | 已就绪，待流量验证 | 排障手 |
| 9 | 🟢 | 文档 | `docs/DISTRIBUTED_ACCESS.md` | ✅ 176 行，多节点接入路径完整 | — | 排障手 |
| 10 | 🟢 | 测试 | 控制面回归 | ✅ 14 passed / 17.13s（`test_fstdd_hub.py` + `test_fstdd_git.py`）；全量基线 592 passed | — | 排障手 |

### 补充：真实状态 vs 账面状态

| 层 | 账面（change 归档 / 测试通过） | 真实（有流量吗） |
|---|---|---|
| L1 数据面 | ✅ 已归档 | ✅ **有真实 push 流量** |
| L2 控制面 | ✅ 已归档 + 部署 | ❌ **零业务流量**（tasks: 0） |
| L3 节点本地 | ✅ 代码 + 测试就绪 | ❌ **从未启用** |

---

## 3. 最小可行闭环判据（全部满足才算"跑通"）

1. **1 台机、1 个真 agent** —— `/nodes` 出现 agent 条目（`fstdd-hub-infra` 不算）
2. **1 个真实子 change** —— hub 原子领取 → 独立 worktree 执行 → push 裸库 → 镜像 `[OK]` → 任务 done
3. 全程无人工代劳
4. **时间盒 2 小时**，超时即失败
5. 复盘一问：**"这比我自己开个终端快吗？"** 否 → 冻结 hub

---

## ✅ 行动清单

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|---------|
| 1 | 对"是否继续投入分布式接入"做决策（推荐 C：限时验证 + 默认冻结） | D哥 | P0 | 本轮 |
| 2 | 若选 C：新立 change 承接「首节点接入 + 首个真实任务」，配 2 小时时间盒与 kill criteria | 执行 agent | P0 | 决策后 |
| 3 | 冻结范围外的扩建：6 AI 并行调度、节点对等与选举、消息系统、跨机 worktree 编排 | — | P1 | 立即生效 |
| 4 | 延后至出现第 2 个真实节点那天：租约过期回收、心跳、幂等键（0 任务时纯自我感动） | — | P1 | 条件触发 |
| 5 | 另立 change 修工具链 3 缺陷（DC-HASH vs `eol=lf`、extract-proposal 空 capabilities、validate AND 误报），**与分布式解绑，不捆绑** | 执行 agent | P2 | 下轮 |

---

## ⚠️ 待完善 / 已知局限

- 本次核验为**只读**，未做写入/接入验证，因此"能否跑通"仍是推断，非实测结论。
- 服务器链路经 SSH 访问，本机已知「远端命令会执行两次」的环境特性；本次仅跑只读命令，结果以端状态为准。
- 工作区当前有一批未提交改动（`docs/DISTRIBUTED_ACCESS.md`、`tools/check_mirror.sh`、
  `tools/deploy_server_bare_repo.sh`、`tools/e2e_mirror_alert.sh`、`upstream/tests/test_mirror_alert_ops.py`），
  均属 `mirror-failure-alert` change 在途产物，本报告未改动。
- `origin` remote URL 内嵌明文 token，核对时注意不外泄。

---

## 📚 成员产出索引

- gstack-investigator（排障手）原始产出：只读健康核查报告 — 组件核验表 10 项 + 三端 sha 收敛证据 + 控制面接口实现确认
- gstack-product-reviewer（产品官）原始产出：Go/No-Go 判断 + A/B/C 三路径对比 + 最小闭环判据 + 砍/延后清单

---

> 本报告由软件工坊 AI 协作生成，关键决策请由工程负责人复核。
